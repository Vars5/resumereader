$(document).ready(function(){
	
	$("#st-search-input").focus();
	$(".focus_").focus();
	//$('#item-69').hover(function(){
	//	$(this).slideUp(200);
	//})
	var $container = $('#container');
	// initialize
	$container.imagesLoaded( function() {
		$container.masonry({
		  columnWidth: 58,
		  itemSelector: '.item',
		  isFitWidth: true
		});
	});
	
	$(function() {
    $( "#dialog" ).dialog({autoOpen: false});
  });

 $( "#share-advice" )
      .button()
      .click(function() {
        $( "#dialog" ).dialog( "open" );
      });

})