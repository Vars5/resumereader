$(document).ready(function(){
	
	$("#st-search-input").focus();
	$(".focus_").focus();
	//$('#item-69').hover(function(){
	//	$(this).slideUp(200);
	//})
	var $container = $('#container');
	// initialize
	$container.imagesLoaded( function() {
		$container.masonry({
		  columnWidth: 58,
		  itemSelector: '.item',
		  isFitWidth: true
		});
	});
	
	$(function() {
    $( "#dialog" ).dialog({
		autoOpen: false,
		modal: true,
		open: function() {
			jQuery('.ui-widget-overlay').bind('click', function() {
				jQuery('#dialog').dialog('close');
			})
		}
		
	});
  });




 $( "#share-advice" ).button().click(function() {
 	$( "#dialog" ).dialog( "open" );
	//$('body').addClass("overlay");
	
  });

//$("#join-button").button().click(function(){
//	$("dialog").disalog("close");
//});

$("form#sign_up_user").bind("ajax:success", function(e, data, status, xhr) {
  if (data.success) {
	$("#dialog").dialog("close");
	$("#save-share").click();
    return $('#submit_comment').slideToggle(1000, "easeOutBack");
  } else {
    return alert('Failure to create user!');
  }
});



})