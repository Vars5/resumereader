<h1> New Bullet point </h1>

<%= form_for [@bulletable, @bullet]  do |f| %>
	<!-- Insert Error form and associated div class later -->
	
	<div class = "field">
		<%= f.text_area :bulletpoint, rows: 3 %>
	</div>
	
	<div class = "actions">
		<%= f.submit %>
	</div>
<% end %>