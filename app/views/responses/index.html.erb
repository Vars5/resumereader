<h1> Answers </h1>

<div id = "Answers">
	<% @responses.each do |response| %>
		<div class = "response">
			<%= simple_format response.answer %>
		</div>
	<% end %>
</div>

<p> <%= link_to "Add Answer", [:new, @responseable, :response ]   %>